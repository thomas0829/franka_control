hydra:
  job:
    chdir: false
  
defaults:
  - log: stdout

  - robot: sim_6_dof
  - env: cube_20x20_fix_ori
  - aug: crop_resize

exp_id: null

seed: 0
gpu_id: 0

episodes: 10
max_episode_length: 100

act_noise_std: 0.001

language_instruction: "pick up the red cube"
